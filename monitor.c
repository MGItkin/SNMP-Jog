#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <stdio.h>
#include <stdlib.h>

void monitor(int if_num, char *interface_values[], int con_num, char *connection_values[]){
    for (int i = 0; i < if_num; i++){
    }

} // monitor(char*[])
